<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WpfApp1"
        Title="Управление студентами (Учебный проект)" Height="450" Width="800">
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <GroupBox Grid.Column="0" Header="Список студентов" Margin="0,0,5,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ListBox Grid.Row="0" 
                         ItemsSource="{Binding Students}" 
                         SelectedItem="{Binding SelectedStudent}" 
                         DisplayMemberPath="FullName" 
                         Margin="5"/>

                <Button Grid.Row="1" Content="Удалить выбранного" 
                        Command="{Binding DeleteCommand}" 
                        Margin="5" Padding="5"/>
            </Grid>
        </GroupBox>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <GroupBox Grid.Row="0" Header="Редактирование деталей" Margin="5,0,0,5">
                <Grid Margin="5" DataContext="{Binding SelectedStudent}">
                    <Grid.Resources>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Margin" Value="0,0,10,0"/>
                        </Style>
                    </Grid.Resources>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Имя:"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Фамилия:"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Возраст:"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Age, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Группа:"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Group, UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>
            </GroupBox>

            <GroupBox Grid.Row="1" Header="Добавить нового студента" Margin="5,0,0,0">
                <Grid Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Имя:"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding NewFirstName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Фамилия:"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding NewLastName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Возраст:"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding NewAge, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Группа:"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding NewGroup, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"/>

                    <Button Grid.Row="4" Grid.ColumnSpan="2" Content="Добавить" 
                            Command="{Binding AddCommand}" 
                            Padding="5" Margin="0,10,0,0"/>
                </Grid>
            </GroupBox>
        </Grid>
    </Grid>
</Window>